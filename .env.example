# Copy this file to .env and fill the values before running migrations or starting the server.

# MySQL connection
DB_HOST=192.168.1.25
DB_PORT=3306
DB_NAME=little_kicks
DB_USER=root
DB_PASS=

# Optional: override defaults used by scripts
# APP_ENV=production
# APP_DEBUG=false
